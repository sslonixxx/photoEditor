<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PhotoEditor" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/silver_gray</item>
        <item name="colorPrimaryVariant">@color/sedate_gray</item>
        <item name="colorOnPrimary">@color/star_white</item>
        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/silver_gray</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/roboto</item>
    </style>
</resources>